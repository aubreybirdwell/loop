# Search order (space-separated). We also scan /media/$USER/* and /run/media/$USER/* automatically.
SEARCH_DIRS="/media/$USER /run/media/$USER /home/$USER/Videos"

# Allowed extensions (case-insensitive)
EXTS="mp4 mov mkv avi m4v"

# If multiple files found, play them all in alphabetical order (1), else only the first match (0)
PLAY_ALL=1

# mpv options tuned for kiosk playback on desktop (X11/Wayland)
MPV_OPTS="--fs --loop=inf --no-osc --no-osd-bar --no-audio-display --really-quiet --hwdec=auto-safe"
